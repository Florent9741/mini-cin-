<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Facades\DB;
use App\Models\Realisateurs;
use App\Models\Salles;

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\App\Models\Films>
 */
class FilmsFactory extends Factory
{
    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition()
    {
        return [
            "nom" => $this->faker->randomElement(
                ["film1", "film2", "film3", "film4", "film5"]
            ),
            "durée" => $this->faker->randomElement(["1H", "1H30", "2H"]),
            "langue" => $this->faker->randomElement(["VF", "VOSTFR"]),
            "description" => $this->faker->lexify('????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????'),
            'id_realisateur' => DB::table('realisateurs')->inRandomOrder()->first()->id,
            'id_salle' => DB::table('salles')->inRandomOrder()->first()->id,
        ];
    }
}
